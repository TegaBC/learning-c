#include <stdio.h>

// Prints the message to console, %s is replaced with the variable "name" (format specifier)
int main() {
    char name[] = "Tega";

    printf("Hello world! Your name is %s.", name);

    return 0;
}